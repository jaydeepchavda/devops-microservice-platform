apiVersion: v1
kind: Service

metadata:
  name: devops-platform-service
  namespace: devops-platform

spec:
  selector:
    app: devops-platform

  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000

  type: NodePort